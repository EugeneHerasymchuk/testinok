#import "./TestFragment.gql"
mutation UpdateTest($id: uuid!, $is_published: Boolean, $questions: jsonb) {
  update_Tests_by_pk(pk_columns: {id: $id}, _set: {is_published: $is_published, questions: $questions}) {
      ...Test
  } 
}